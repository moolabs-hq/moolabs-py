# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK covering billing (CLS) and usage (Meter) operations. Both surfaces share one client and one auth flow.

    The version of the OpenAPI document: 0.1.0-rc1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from moolabs.models.tax_config import TaxConfig
from typing import Optional, Set
from typing_extensions import Self

class BillingWorkflowInvoicingSettings(BaseModel):
    """
    BillingWorkflowInvoicingSettings represents the invoice settings for a billing workflow
    """ # noqa: E501
    auto_advance: Optional[StrictBool] = Field(default=True, description="Whether to automatically issue the invoice after the draftPeriod has passed.", alias="autoAdvance")
    draft_period: Optional[StrictStr] = Field(default='P0D', description="The period for the invoice to be kept in draft status for manual reviews.", alias="draftPeriod")
    due_after: Optional[StrictStr] = Field(default='P30D', description="The period after which the invoice is due. With some payment solutions it's only applicable for manual collection method.", alias="dueAfter")
    progressive_billing: Optional[StrictBool] = Field(default=False, description="Should progressive billing be allowed for this workflow?", alias="progressiveBilling")
    default_tax_config: Optional[TaxConfig] = Field(default=None, description="Default tax configuration to apply to the invoices.", alias="defaultTaxConfig")
    __properties: ClassVar[List[str]] = ["autoAdvance", "draftPeriod", "dueAfter", "progressiveBilling", "defaultTaxConfig"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BillingWorkflowInvoicingSettings from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of default_tax_config
        if self.default_tax_config:
            _dict['defaultTaxConfig'] = self.default_tax_config.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BillingWorkflowInvoicingSettings from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "autoAdvance": obj.get("autoAdvance") if obj.get("autoAdvance") is not None else True,
            "draftPeriod": obj.get("draftPeriod") if obj.get("draftPeriod") is not None else 'P0D',
            "dueAfter": obj.get("dueAfter") if obj.get("dueAfter") is not None else 'P30D',
            "progressiveBilling": obj.get("progressiveBilling") if obj.get("progressiveBilling") is not None else False,
            "defaultTaxConfig": TaxConfig.from_dict(obj["defaultTaxConfig"]) if obj.get("defaultTaxConfig") is not None else None
        })
        return _obj


