# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.2.0-rc9
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class ChannelPolicySchema(BaseModel):
    """
    Per-channel rate limits and quiet hours.
    """ # noqa: E501
    max_per_week: Optional[Annotated[int, Field(le=20, strict=True, ge=1)]] = 3
    quiet_hours_start: Optional[Annotated[str, Field(strict=True)]] = '21:00'
    quiet_hours_end: Optional[Annotated[str, Field(strict=True)]] = '08:00'
    __properties: ClassVar[List[str]] = ["max_per_week", "quiet_hours_start", "quiet_hours_end"]

    @field_validator('quiet_hours_start')
    def quiet_hours_start_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\d{2}:\d{2}$", value):
            raise ValueError(r"must validate the regular expression /^\d{2}:\d{2}$/")
        return value

    @field_validator('quiet_hours_end')
    def quiet_hours_end_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\d{2}:\d{2}$", value):
            raise ValueError(r"must validate the regular expression /^\d{2}:\d{2}$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ChannelPolicySchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ChannelPolicySchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "max_per_week": obj.get("max_per_week") if obj.get("max_per_week") is not None else 3,
            "quiet_hours_start": obj.get("quiet_hours_start") if obj.get("quiet_hours_start") is not None else '21:00',
            "quiet_hours_end": obj.get("quiet_hours_end") if obj.get("quiet_hours_end") is not None else '08:00'
        })
        return _obj


