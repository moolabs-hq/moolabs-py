# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.3.0-rc1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from moolabs.models.channel_policy_schema import ChannelPolicySchema
from moolabs.models.stage_schema import StageSchema
from typing import Optional, Set
from typing_extensions import Self

class StrategyUpdateRequest(BaseModel):
    """
    StrategyUpdateRequest
    """ # noqa: E501
    name: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=255)]] = None
    risk_tier: Optional[Annotated[str, Field(strict=True)]] = None
    stages: Optional[List[StageSchema]] = None
    channel_policy: Optional[Dict[str, ChannelPolicySchema]] = None
    segment_filters: Optional[Dict[str, Any]] = None
    escalation_policy: Optional[Dict[str, Any]] = None
    priority: Optional[Annotated[int, Field(le=1000, strict=True, ge=1)]] = None
    is_default: Optional[StrictBool] = None
    __properties: ClassVar[List[str]] = ["name", "risk_tier", "stages", "channel_policy", "segment_filters", "escalation_policy", "priority", "is_default"]

    @field_validator('risk_tier')
    def risk_tier_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^(low|medium|high|critical)$", value):
            raise ValueError(r"must validate the regular expression /^(low|medium|high|critical)$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of StrategyUpdateRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in stages (list)
        _items = []
        if self.stages:
            for _item_stages in self.stages:
                if _item_stages:
                    _items.append(_item_stages.to_dict())
            _dict['stages'] = _items
        # override the default output from pydantic by calling `to_dict()` of each value in channel_policy (dict)
        _field_dict = {}
        if self.channel_policy:
            for _key_channel_policy in self.channel_policy:
                if self.channel_policy[_key_channel_policy]:
                    _field_dict[_key_channel_policy] = self.channel_policy[_key_channel_policy].to_dict()
            _dict['channel_policy'] = _field_dict
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of StrategyUpdateRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "risk_tier": obj.get("risk_tier"),
            "stages": [StageSchema.from_dict(_item) for _item in obj["stages"]] if obj.get("stages") is not None else None,
            "channel_policy": dict(
                (_k, ChannelPolicySchema.from_dict(_v))
                for _k, _v in obj["channel_policy"].items()
            )
            if obj.get("channel_policy") is not None
            else None,
            "segment_filters": obj.get("segment_filters"),
            "escalation_policy": obj.get("escalation_policy"),
            "priority": obj.get("priority"),
            "is_default": obj.get("is_default")
        })
        return _obj


