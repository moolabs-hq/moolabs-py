# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK covering billing (CLS) and usage (Meter) operations. Both surfaces share one client and one auth flow.

    The version of the OpenAPI document: 0.0.0-rc2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class FilterString(BaseModel):
    """
    A filter for a string field.
    """ # noqa: E501
    eq: Optional[StrictStr] = Field(default=None, description="The field must be equal to the provided value.", alias="$eq")
    ne: Optional[StrictStr] = Field(default=None, description="The field must not be equal to the provided value.", alias="$ne")
    var_in: Optional[List[StrictStr]] = Field(default=None, description="The field must be in the provided list of values.", alias="$in")
    nin: Optional[List[StrictStr]] = Field(default=None, description="The field must not be in the provided list of values.", alias="$nin")
    like: Optional[StrictStr] = Field(default=None, description="The field must match the provided value.", alias="$like")
    nlike: Optional[StrictStr] = Field(default=None, description="The field must not match the provided value.", alias="$nlike")
    ilike: Optional[StrictStr] = Field(default=None, description="The field must match the provided value, ignoring case.", alias="$ilike")
    nilike: Optional[StrictStr] = Field(default=None, description="The field must not match the provided value, ignoring case.", alias="$nilike")
    gt: Optional[StrictStr] = Field(default=None, description="The field must be greater than the provided value.", alias="$gt")
    gte: Optional[StrictStr] = Field(default=None, description="The field must be greater than or equal to the provided value.", alias="$gte")
    lt: Optional[StrictStr] = Field(default=None, description="The field must be less than the provided value.", alias="$lt")
    lte: Optional[StrictStr] = Field(default=None, description="The field must be less than or equal to the provided value.", alias="$lte")
    var_and: Optional[List[FilterString]] = Field(default=None, description="Provide a list of filters to be combined with a logical AND.", alias="$and")
    var_or: Optional[List[FilterString]] = Field(default=None, description="Provide a list of filters to be combined with a logical OR.", alias="$or")
    __properties: ClassVar[List[str]] = ["$eq", "$ne", "$in", "$nin", "$like", "$nlike", "$ilike", "$nilike", "$gt", "$gte", "$lt", "$lte", "$and", "$or"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FilterString from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in var_and (list)
        _items = []
        if self.var_and:
            for _item_var_and in self.var_and:
                if _item_var_and:
                    _items.append(_item_var_and.to_dict())
            _dict['$and'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in var_or (list)
        _items = []
        if self.var_or:
            for _item_var_or in self.var_or:
                if _item_var_or:
                    _items.append(_item_var_or.to_dict())
            _dict['$or'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FilterString from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "$eq": obj.get("$eq"),
            "$ne": obj.get("$ne"),
            "$in": obj.get("$in"),
            "$nin": obj.get("$nin"),
            "$like": obj.get("$like"),
            "$nlike": obj.get("$nlike"),
            "$ilike": obj.get("$ilike"),
            "$nilike": obj.get("$nilike"),
            "$gt": obj.get("$gt"),
            "$gte": obj.get("$gte"),
            "$lt": obj.get("$lt"),
            "$lte": obj.get("$lte"),
            "$and": [FilterString.from_dict(_item) for _item in obj["$and"]] if obj.get("$and") is not None else None,
            "$or": [FilterString.from_dict(_item) for _item in obj["$or"]] if obj.get("$or") is not None else None
        })
        return _obj

# TODO: Rewrite to not use raise_errors
FilterString.model_rebuild(raise_errors=False)

