# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK covering billing (CLS) and usage (Meter) operations. Both surfaces share one client and one auth flow.

    The version of the OpenAPI document: 0.2.0-rc1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from moolabs.models.dynamic_price import DynamicPrice
from moolabs.models.flat_price import FlatPrice
from moolabs.models.package_price import PackagePrice
from moolabs.models.tiered_price import TieredPrice
from moolabs.models.unit_price import UnitPrice
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

PRICE_ONE_OF_SCHEMAS = ["DynamicPrice", "FlatPrice", "PackagePrice", "TieredPrice", "UnitPrice"]

class Price(BaseModel):
    """
    Price. One of: flat, unit, or tiered.
    """
    # data type: FlatPrice
    oneof_schema_1_validator: Optional[FlatPrice] = None
    # data type: UnitPrice
    oneof_schema_2_validator: Optional[UnitPrice] = None
    # data type: TieredPrice
    oneof_schema_3_validator: Optional[TieredPrice] = None
    # data type: DynamicPrice
    oneof_schema_4_validator: Optional[DynamicPrice] = None
    # data type: PackagePrice
    oneof_schema_5_validator: Optional[PackagePrice] = None
    actual_instance: Optional[Union[DynamicPrice, FlatPrice, PackagePrice, TieredPrice, UnitPrice]] = None
    one_of_schemas: Set[str] = { "DynamicPrice", "FlatPrice", "PackagePrice", "TieredPrice", "UnitPrice" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Price.model_construct()
        error_messages = []
        match = 0
        # validate data type: FlatPrice
        if not isinstance(v, FlatPrice):
            error_messages.append(f"Error! Input type `{type(v)}` is not `FlatPrice`")
        else:
            match += 1
        # validate data type: UnitPrice
        if not isinstance(v, UnitPrice):
            error_messages.append(f"Error! Input type `{type(v)}` is not `UnitPrice`")
        else:
            match += 1
        # validate data type: TieredPrice
        if not isinstance(v, TieredPrice):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TieredPrice`")
        else:
            match += 1
        # validate data type: DynamicPrice
        if not isinstance(v, DynamicPrice):
            error_messages.append(f"Error! Input type `{type(v)}` is not `DynamicPrice`")
        else:
            match += 1
        # validate data type: PackagePrice
        if not isinstance(v, PackagePrice):
            error_messages.append(f"Error! Input type `{type(v)}` is not `PackagePrice`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Price with oneOf schemas: DynamicPrice, FlatPrice, PackagePrice, TieredPrice, UnitPrice. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Price with oneOf schemas: DynamicPrice, FlatPrice, PackagePrice, TieredPrice, UnitPrice. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into FlatPrice
        try:
            instance.actual_instance = FlatPrice.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into UnitPrice
        try:
            instance.actual_instance = UnitPrice.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into TieredPrice
        try:
            instance.actual_instance = TieredPrice.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into DynamicPrice
        try:
            instance.actual_instance = DynamicPrice.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into PackagePrice
        try:
            instance.actual_instance = PackagePrice.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Price with oneOf schemas: DynamicPrice, FlatPrice, PackagePrice, TieredPrice, UnitPrice. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Price with oneOf schemas: DynamicPrice, FlatPrice, PackagePrice, TieredPrice, UnitPrice. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], DynamicPrice, FlatPrice, PackagePrice, TieredPrice, UnitPrice]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


