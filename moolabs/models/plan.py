# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.2.0-rc7
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from moolabs.models.alignment import Alignment
from moolabs.models.credit_config import CreditConfig
from moolabs.models.meter_validation_error import MeterValidationError
from moolabs.models.plan_phase import PlanPhase
from moolabs.models.plan_status import PlanStatus
from moolabs.models.pro_rating_config import ProRatingConfig
from typing import Optional, Set
from typing_extensions import Self

class Plan(BaseModel):
    """
    Plans provide a template for subscriptions.
    """ # noqa: E501
    id: Annotated[str, Field(strict=True)] = Field(description="A unique identifier for the resource.")
    name: Annotated[str, Field(min_length=1, strict=True, max_length=256)] = Field(description="Human-readable name for the resource. Between 1 and 256 characters.")
    description: Optional[Annotated[str, Field(strict=True, max_length=1024)]] = Field(default=None, description="Optional description of the resource. Maximum 1024 characters.")
    metadata: Optional[Dict[str, StrictStr]] = Field(default=None, description="Additional metadata for the resource.")
    created_at: datetime = Field(description="Timestamp of when the resource was created.", alias="createdAt")
    updated_at: datetime = Field(description="Timestamp of when the resource was last updated.", alias="updatedAt")
    deleted_at: Optional[datetime] = Field(default=None, description="Timestamp of when the resource was permanently deleted.", alias="deletedAt")
    key: Annotated[str, Field(min_length=1, strict=True, max_length=64)] = Field(description="A semi-unique identifier for the resource.")
    alignment: Optional[Alignment] = Field(default=None, description="Alignment configuration for the plan.")
    version: Annotated[int, Field(strict=True, ge=1)] = Field(description="Version of the plan. Incremented when the plan is updated.")
    currency: Annotated[str, Field(min_length=3, strict=True, max_length=3)] = Field(description="The currency code of the plan.")
    billing_cadence: StrictStr = Field(description="The default billing cadence for subscriptions using this plan. Defines how often customers are billed using ISO8601 duration format. Examples: \"P1M\" (monthly), \"P3M\" (quarterly), \"P1Y\" (annually).", alias="billingCadence")
    pro_rating_config: Optional[ProRatingConfig] = Field(default=None, description="Default pro-rating configuration for subscriptions using this plan.", alias="proRatingConfig")
    credit_config: Optional[CreditConfig] = Field(default=None, description="Default credit allowance configuration for subscriptions using this plan (first-class column).", alias="creditConfig")
    effective_from: Optional[datetime] = Field(default=None, description="The date and time when the plan becomes effective. When not specified, the plan is a draft.", alias="effectiveFrom")
    effective_to: Optional[datetime] = Field(default=None, description="The date and time when the plan is no longer effective. When not specified, the plan is effective indefinitely.", alias="effectiveTo")
    status: PlanStatus = Field(description="The status of the plan. Computed based on the effective start and end dates: - draft = no effectiveFrom - active = effectiveFrom <= now < effectiveTo - archived / inactive = effectiveTo <= now - scheduled = now < effectiveFrom < effectiveTo")
    phases: Annotated[List[PlanPhase], Field(min_length=1)] = Field(description="The plan phase or pricing ramp allows changing a plan's rate cards over time as a subscription progresses. A phase switch occurs only at the end of a billing period, ensuring that a single subscription invoice will not include charges from different phase prices.")
    validation_errors: List[MeterValidationError] = Field(description="List of validation errors.", alias="validationErrors")
    __properties: ClassVar[List[str]] = ["id", "name", "description", "metadata", "createdAt", "updatedAt", "deletedAt", "key", "alignment", "version", "currency", "billingCadence", "proRatingConfig", "creditConfig", "effectiveFrom", "effectiveTo", "status", "phases", "validationErrors"]

    @field_validator('id')
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[0-7][0-9A-HJKMNP-TV-Za-hjkmnp-tv-z]{25}$", value):
            raise ValueError(r"must validate the regular expression /^[0-7][0-9A-HJKMNP-TV-Za-hjkmnp-tv-z]{25}$/")
        return value

    @field_validator('key')
    def key_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[a-z0-9]+(?:_[a-z0-9]+)*$", value):
            raise ValueError(r"must validate the regular expression /^[a-z0-9]+(?:_[a-z0-9]+)*$/")
        return value

    @field_validator('currency')
    def currency_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[A-Z]{3}$", value):
            raise ValueError(r"must validate the regular expression /^[A-Z]{3}$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Plan from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "id",
            "created_at",
            "updated_at",
            "deleted_at",
            "version",
            "effective_from",
            "effective_to",
            "status",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of alignment
        if self.alignment:
            _dict['alignment'] = self.alignment.to_dict()
        # override the default output from pydantic by calling `to_dict()` of pro_rating_config
        if self.pro_rating_config:
            _dict['proRatingConfig'] = self.pro_rating_config.to_dict()
        # override the default output from pydantic by calling `to_dict()` of credit_config
        if self.credit_config:
            _dict['creditConfig'] = self.credit_config.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in phases (list)
        _items = []
        if self.phases:
            for _item_phases in self.phases:
                if _item_phases:
                    _items.append(_item_phases.to_dict())
            _dict['phases'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in validation_errors (list)
        _items = []
        if self.validation_errors:
            for _item_validation_errors in self.validation_errors:
                if _item_validation_errors:
                    _items.append(_item_validation_errors.to_dict())
            _dict['validationErrors'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Plan from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "name": obj.get("name"),
            "description": obj.get("description"),
            "metadata": obj.get("metadata"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt"),
            "deletedAt": obj.get("deletedAt"),
            "key": obj.get("key"),
            "alignment": Alignment.from_dict(obj["alignment"]) if obj.get("alignment") is not None else None,
            "version": obj.get("version") if obj.get("version") is not None else 1,
            "currency": obj.get("currency"),
            "billingCadence": obj.get("billingCadence"),
            "proRatingConfig": ProRatingConfig.from_dict(obj["proRatingConfig"]) if obj.get("proRatingConfig") is not None else None,
            "creditConfig": CreditConfig.from_dict(obj["creditConfig"]) if obj.get("creditConfig") is not None else None,
            "effectiveFrom": obj.get("effectiveFrom"),
            "effectiveTo": obj.get("effectiveTo"),
            "status": obj.get("status"),
            "phases": [PlanPhase.from_dict(_item) for _item in obj["phases"]] if obj.get("phases") is not None else None,
            "validationErrors": [MeterValidationError.from_dict(_item) for _item in obj["validationErrors"]] if obj.get("validationErrors") is not None else None
        })
        return _obj


