# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.3.0-rc4
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from moolabs.models.billing_workflow_collection_alignment import BillingWorkflowCollectionAlignment
from typing import Optional, Set
from typing_extensions import Self

class BillingWorkflowCollectionSettings(BaseModel):
    """
    Workflow collection specifies how to collect the pending line items for an invoice
    """ # noqa: E501
    alignment: Optional[BillingWorkflowCollectionAlignment] = Field(default=None, description="The alignment for collecting the pending line items into an invoice.")
    interval: Optional[StrictStr] = Field(default='PT1H', description="This grace period can be used to delay the collection of the pending line items specified in alignment.  This is useful, in case of multiple subscriptions having slightly different billing periods.")
    __properties: ClassVar[List[str]] = ["alignment", "interval"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BillingWorkflowCollectionSettings from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of alignment
        if self.alignment:
            _dict['alignment'] = self.alignment.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BillingWorkflowCollectionSettings from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "alignment": BillingWorkflowCollectionAlignment.from_dict(obj["alignment"]) if obj.get("alignment") is not None else None,
            "interval": obj.get("interval") if obj.get("interval") is not None else 'PT1H'
        })
        return _obj


