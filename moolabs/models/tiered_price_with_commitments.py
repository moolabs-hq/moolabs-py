# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK covering billing (CLS) and usage (Meter) operations. Both surfaces share one client and one auth flow.

    The version of the OpenAPI document: 0.0.0-rc2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from moolabs.models.price_tier import PriceTier
from moolabs.models.tiered_price_mode import TieredPriceMode
from typing import Optional, Set
from typing_extensions import Self

class TieredPriceWithCommitments(BaseModel):
    """
    Tiered price with spend commitments.
    """ # noqa: E501
    type: StrictStr = Field(description="The type of the price.  One of: flat, unit, or tiered.")
    mode: TieredPriceMode = Field(description="Defines if the tiering mode is volume-based or graduated: - In `volume`-based tiering, the maximum quantity within a period determines the per unit price. - In `graduated` tiering, pricing can change as the quantity grows.")
    tiers: Annotated[List[PriceTier], Field(min_length=1)] = Field(description="The tiers of the tiered price. At least one price component is required in each tier.")
    minimum_amount: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The customer is committed to spend at least the amount.", alias="minimumAmount")
    maximum_amount: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The customer is limited to spend at most the amount.", alias="maximumAmount")
    __properties: ClassVar[List[str]] = ["type", "mode", "tiers", "minimumAmount", "maximumAmount"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['tiered']):
            raise ValueError("must be one of enum values ('tiered')")
        return value

    @field_validator('minimum_amount')
    def minimum_amount_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\-?[0-9]+(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^\-?[0-9]+(\.[0-9]+)?$/")
        return value

    @field_validator('maximum_amount')
    def maximum_amount_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\-?[0-9]+(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^\-?[0-9]+(\.[0-9]+)?$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TieredPriceWithCommitments from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in tiers (list)
        _items = []
        if self.tiers:
            for _item_tiers in self.tiers:
                if _item_tiers:
                    _items.append(_item_tiers.to_dict())
            _dict['tiers'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TieredPriceWithCommitments from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "mode": obj.get("mode"),
            "tiers": [PriceTier.from_dict(_item) for _item in obj["tiers"]] if obj.get("tiers") is not None else None,
            "minimumAmount": obj.get("minimumAmount"),
            "maximumAmount": obj.get("maximumAmount")
        })
        return _obj


