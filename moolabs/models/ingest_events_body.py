# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.3.0-rc4
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
from inspect import getfullargspec
import json
import pprint
import re  # noqa: F401
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import List, Optional
from moolabs.models.event import Event
from typing import Union, Any, List, Set, TYPE_CHECKING, Optional, Dict
from typing_extensions import Literal, Self
from pydantic import Field

INGESTEVENTSBODY_ANY_OF_SCHEMAS = ["Event", "List[Event]"]

class IngestEventsBody(BaseModel):
    """
    The body of the events request. Either a single event or a batch of events.
    """

    # data type: Event
    anyof_schema_1_validator: Optional[Event] = None
    # data type: List[Event]
    anyof_schema_2_validator: Optional[List[Event]] = None
    if TYPE_CHECKING:
        actual_instance: Optional[Union[Event, List[Event]]] = None
    else:
        actual_instance: Any = None
    any_of_schemas: Set[str] = { "Event", "List[Event]" }

    # CUSTOMIZED (moolabs override): `extra="forbid"` added so a caller
    # passing unknown kwargs (e.g. `ObservedTotalCost(amount="0.0001",
    # currency="USD")` against an anyOf[number, string] wrapper) gets a
    # ValidationError instead of a silently-empty model whose
    # `actual_instance` stays None. Stock template omits this and the
    # silent-construct path was the proximate cause of the misdiagnosis
    # chain documented in moolabs/docs/superpowers/reviews/
    # 2026-05-18-sdk-bugs-missed-detection-postmortem.md.
    #
    # Safe for the deserialization path: `from_json` / `from_dict` below
    # uses `cls.model_construct()` which bypasses validation entirely,
    # so wire-deserialized payloads are unaffected — only direct
    # __init__ / model_validate calls now enforce extras.
    model_config = {
        "validate_assignment": True,
        "protected_namespaces": (),
        "extra": "forbid",
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_anyof(cls, v):
        instance = IngestEventsBody.model_construct()
        error_messages = []
        # validate data type: Event
        if not isinstance(v, Event):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Event`")
        else:
            return v

        # validate data type: List[Event]
        try:
            instance.anyof_schema_2_validator = v
            return v
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        if error_messages:
            # no match
            raise ValueError("No match found when setting the actual_instance in IngestEventsBody with anyOf schemas: Event, List[Event]. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Dict[str, Any]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        # anyof_schema_1_validator: Optional[Event] = None
        try:
            instance.actual_instance = Event.from_json(json_str)
            return instance
        except (ValidationError, ValueError) as e:
             error_messages.append(str(e))
        # deserialize data into List[Event]
        try:
            # validation
            instance.anyof_schema_2_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.anyof_schema_2_validator
            return instance
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if error_messages:
            # no match
            raise ValueError("No match found when deserializing the JSON string into IngestEventsBody with anyOf schemas: Event, List[Event]. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], Event, List[Event]]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


