# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK covering billing (CLS) and usage (Meter) operations. Both surfaces share one client and one auth flow.

    The version of the OpenAPI document: 0.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from moolabs.models.custom_invoicing_tax_config import CustomInvoicingTaxConfig
from moolabs.models.stripe_tax_config import StripeTaxConfig
from moolabs.models.tax_behavior import TaxBehavior
from typing import Optional, Set
from typing_extensions import Self

class TaxConfig(BaseModel):
    """
    Set of provider specific tax configs.
    """ # noqa: E501
    behavior: Optional[TaxBehavior] = Field(default=None, description="Tax behavior.  If not specified the billing profile is used to determine the tax behavior. If not specified in the billing profile, the provider's default behavior is used.")
    stripe: Optional[StripeTaxConfig] = Field(default=None, description="Stripe tax config.")
    custom_invoicing: Optional[CustomInvoicingTaxConfig] = Field(default=None, description="Custom invoicing tax config.", alias="customInvoicing")
    __properties: ClassVar[List[str]] = ["behavior", "stripe", "customInvoicing"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TaxConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of stripe
        if self.stripe:
            _dict['stripe'] = self.stripe.to_dict()
        # override the default output from pydantic by calling `to_dict()` of custom_invoicing
        if self.custom_invoicing:
            _dict['customInvoicing'] = self.custom_invoicing.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TaxConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "behavior": obj.get("behavior"),
            "stripe": StripeTaxConfig.from_dict(obj["stripe"]) if obj.get("stripe") is not None else None,
            "customInvoicing": CustomInvoicingTaxConfig.from_dict(obj["customInvoicing"]) if obj.get("customInvoicing") is not None else None
        })
        return _obj


