# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.3.0-rc1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from moolabs.models.custom_invoicing_app import CustomInvoicingApp
from moolabs.models.sandbox_app import SandboxApp
from moolabs.models.stripe_app import StripeApp
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

APP_ONE_OF_SCHEMAS = ["CustomInvoicingApp", "SandboxApp", "StripeApp"]

class App(BaseModel):
    """
    App. One of: stripe
    """
    # data type: StripeApp
    oneof_schema_1_validator: Optional[StripeApp] = None
    # data type: SandboxApp
    oneof_schema_2_validator: Optional[SandboxApp] = None
    # data type: CustomInvoicingApp
    oneof_schema_3_validator: Optional[CustomInvoicingApp] = None
    actual_instance: Optional[Union[CustomInvoicingApp, SandboxApp, StripeApp]] = None
    one_of_schemas: Set[str] = { "CustomInvoicingApp", "SandboxApp", "StripeApp" }

    # CUSTOMIZED (moolabs override): `extra="forbid"` added so a caller
    # passing unknown kwargs to a oneOf wrapper class gets a
    # ValidationError instead of a silently-empty model whose
    # `actual_instance` stays None. Mirrors the same fix applied to
    # model_anyof.mustache — see comments there and the postmortem at
    # docs/superpowers/reviews/2026-05-18-sdk-bugs-missed-detection-postmortem.md.
    #
    # Safe for the deserialization path: `from_json` / `from_dict` below
    # uses `cls.model_construct()` which bypasses validation entirely.
    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
        extra="forbid",
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = App.model_construct()
        error_messages = []
        match = 0
        # validate data type: StripeApp
        if not isinstance(v, StripeApp):
            error_messages.append(f"Error! Input type `{type(v)}` is not `StripeApp`")
        else:
            match += 1
        # validate data type: SandboxApp
        if not isinstance(v, SandboxApp):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SandboxApp`")
        else:
            match += 1
        # validate data type: CustomInvoicingApp
        if not isinstance(v, CustomInvoicingApp):
            error_messages.append(f"Error! Input type `{type(v)}` is not `CustomInvoicingApp`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in App with oneOf schemas: CustomInvoicingApp, SandboxApp, StripeApp. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in App with oneOf schemas: CustomInvoicingApp, SandboxApp, StripeApp. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into StripeApp
        try:
            instance.actual_instance = StripeApp.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into SandboxApp
        try:
            instance.actual_instance = SandboxApp.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into CustomInvoicingApp
        try:
            instance.actual_instance = CustomInvoicingApp.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into App with oneOf schemas: CustomInvoicingApp, SandboxApp, StripeApp. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into App with oneOf schemas: CustomInvoicingApp, SandboxApp, StripeApp. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], CustomInvoicingApp, SandboxApp, StripeApp]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


