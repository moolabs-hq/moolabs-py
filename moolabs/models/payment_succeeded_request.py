# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK covering billing (CLS) and usage (Meter) operations. Both surfaces share one client and one auth flow.

    The version of the OpenAPI document: 0.0.0-rc6
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class PaymentSucceededRequest(BaseModel):
    """
    Request for payment succeeded callback.
    """ # noqa: E501
    tenant_id: StrictStr = Field(description="Tenant identifier")
    pool_id: StrictStr = Field(description="Pool identifier")
    wallet_id: StrictStr = Field(description="Wallet identifier")
    payment_id: StrictStr = Field(description="Payment identifier (idempotency key)")
    amount_micros: StrictInt = Field(description="Amount in micros")
    fx_rate_version: Optional[StrictStr] = None
    credits_per_usd_micros: Optional[Annotated[int, Field(strict=True, ge=0)]] = None
    effective_at: Optional[datetime] = None
    __properties: ClassVar[List[str]] = ["tenant_id", "pool_id", "wallet_id", "payment_id", "amount_micros", "fx_rate_version", "credits_per_usd_micros", "effective_at"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PaymentSucceededRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if fx_rate_version (nullable) is None
        # and model_fields_set contains the field
        if self.fx_rate_version is None and "fx_rate_version" in self.model_fields_set:
            _dict['fx_rate_version'] = None

        # set to None if credits_per_usd_micros (nullable) is None
        # and model_fields_set contains the field
        if self.credits_per_usd_micros is None and "credits_per_usd_micros" in self.model_fields_set:
            _dict['credits_per_usd_micros'] = None

        # set to None if effective_at (nullable) is None
        # and model_fields_set contains the field
        if self.effective_at is None and "effective_at" in self.model_fields_set:
            _dict['effective_at'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PaymentSucceededRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "tenant_id": obj.get("tenant_id"),
            "pool_id": obj.get("pool_id"),
            "wallet_id": obj.get("wallet_id"),
            "payment_id": obj.get("payment_id"),
            "amount_micros": obj.get("amount_micros"),
            "fx_rate_version": obj.get("fx_rate_version"),
            "credits_per_usd_micros": obj.get("credits_per_usd_micros"),
            "effective_at": obj.get("effective_at")
        })
        return _obj


