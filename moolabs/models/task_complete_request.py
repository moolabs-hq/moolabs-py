# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.3.0-rc4
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class TaskCompleteRequest(BaseModel):
    """
    POST /tasks/{id}/complete  ``resolution`` and ``payload`` are the legacy free-form fields used for operator notes / arbitrary annotations.  ``dispatch_payload`` is the structured side-effect input read by ``app.services.task_dispatch`` (e.g. ``promised_amount_micros`` for a ``confirm_ptp`` completion). Keeping the two split avoids dispatch handlers accidentally reading operator-supplied free-text where they expected a structured field.
    """ # noqa: E501
    resolution: Optional[Annotated[str, Field(strict=True, max_length=2000)]] = None
    payload: Optional[Dict[str, Any]] = None
    dispatch_payload: Optional[Dict[str, Any]] = Field(default=None, description="Structured input consumed by app.services.task_dispatch — shape depends on task_type (see task_dispatch handlers).")
    __properties: ClassVar[List[str]] = ["resolution", "payload", "dispatch_payload"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of TaskCompleteRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of TaskCompleteRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "resolution": obj.get("resolution"),
            "payload": obj.get("payload"),
            "dispatch_payload": obj.get("dispatch_payload")
        })
        return _obj


