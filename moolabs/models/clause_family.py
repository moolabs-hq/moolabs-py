# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.3.0-rc2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from moolabs.models.clause_citations import ClauseCitations
from moolabs.models.clause_position import ClausePosition
from moolabs.models.deal_context import DealContext
from typing import Optional, Set
from typing_extensions import Self

class ClauseFamily(BaseModel):
    """
    A logical group of related clauses, each with multiple negotiating positions.
    """ # noqa: E501
    family_id: StrictStr
    display_name: StrictStr
    owner_persona: StrictStr
    required: Optional[StrictBool] = False
    detector: Dict[str, Any]
    positions: Dict[str, ClausePosition]
    deal_context: Optional[DealContext] = None
    citations: Optional[ClauseCitations] = None
    __properties: ClassVar[List[str]] = ["family_id", "display_name", "owner_persona", "required", "detector", "positions", "deal_context", "citations"]

    @field_validator('owner_persona')
    def owner_persona_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['legal', 'infosec', 'finance']):
            raise ValueError("must be one of enum values ('legal', 'infosec', 'finance')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ClauseFamily from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in positions (dict)
        _field_dict = {}
        if self.positions:
            for _key_positions in self.positions:
                if self.positions[_key_positions]:
                    _field_dict[_key_positions] = self.positions[_key_positions].to_dict()
            _dict['positions'] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of deal_context
        if self.deal_context:
            _dict['deal_context'] = self.deal_context.to_dict()
        # override the default output from pydantic by calling `to_dict()` of citations
        if self.citations:
            _dict['citations'] = self.citations.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ClauseFamily from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "family_id": obj.get("family_id"),
            "display_name": obj.get("display_name"),
            "owner_persona": obj.get("owner_persona"),
            "required": obj.get("required") if obj.get("required") is not None else False,
            "detector": obj.get("detector"),
            "positions": dict(
                (_k, ClausePosition.from_dict(_v))
                for _k, _v in obj["positions"].items()
            )
            if obj.get("positions") is not None
            else None,
            "deal_context": DealContext.from_dict(obj["deal_context"]) if obj.get("deal_context") is not None else None,
            "citations": ClauseCitations.from_dict(obj["citations"]) if obj.get("citations") is not None else None
        })
        return _obj


