# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.2.0-rc5
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class StageSchema(BaseModel):
    """
    One stage in a dunning strategy.
    """ # noqa: E501
    stage: Annotated[int, Field(strict=True, ge=1)] = Field(description="Stage sequence number")
    name: Annotated[str, Field(min_length=1, strict=True, max_length=100)]
    days_overdue_trigger: Annotated[int, Field(strict=True, ge=0)]
    channel: Annotated[str, Field(strict=True)]
    tone: Optional[Annotated[str, Field(strict=True)]] = 'professional'
    autonomy_min: Optional[Annotated[int, Field(le=5, strict=True, ge=1)]] = 1
    template_key: Annotated[str, Field(min_length=1, strict=True, max_length=100)]
    __properties: ClassVar[List[str]] = ["stage", "name", "days_overdue_trigger", "channel", "tone", "autonomy_min", "template_key"]

    @field_validator('channel')
    def channel_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^(email|sms|phone|letter)$", value):
            raise ValueError(r"must validate the regular expression /^(email|sms|phone|letter)$/")
        return value

    @field_validator('tone')
    def tone_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^(friendly|professional|firm|formal|urgent)$", value):
            raise ValueError(r"must validate the regular expression /^(friendly|professional|firm|formal|urgent)$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of StageSchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of StageSchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "stage": obj.get("stage"),
            "name": obj.get("name"),
            "days_overdue_trigger": obj.get("days_overdue_trigger"),
            "channel": obj.get("channel"),
            "tone": obj.get("tone") if obj.get("tone") is not None else 'professional',
            "autonomy_min": obj.get("autonomy_min") if obj.get("autonomy_min") is not None else 1,
            "template_key": obj.get("template_key")
        })
        return _obj


