# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.

    The version of the OpenAPI document: 0.2.0-rc10
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class PackagePriceWithCommitments(BaseModel):
    """
    Package price with spend commitments.
    """ # noqa: E501
    type: StrictStr = Field(description="The type of the price.")
    amount: Annotated[str, Field(strict=True)] = Field(description="The price of one package.")
    quantity_per_package: Annotated[str, Field(strict=True)] = Field(description="The quantity per package.", alias="quantityPerPackage")
    minimum_amount: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The customer is committed to spend at least the amount.", alias="minimumAmount")
    maximum_amount: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The customer is limited to spend at most the amount.", alias="maximumAmount")
    __properties: ClassVar[List[str]] = ["type", "amount", "quantityPerPackage", "minimumAmount", "maximumAmount"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['package']):
            raise ValueError("must be one of enum values ('package')")
        return value

    @field_validator('amount')
    def amount_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^\-?[0-9]+(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^\-?[0-9]+(\.[0-9]+)?$/")
        return value

    @field_validator('quantity_per_package')
    def quantity_per_package_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^\-?[0-9]+(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^\-?[0-9]+(\.[0-9]+)?$/")
        return value

    @field_validator('minimum_amount')
    def minimum_amount_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\-?[0-9]+(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^\-?[0-9]+(\.[0-9]+)?$/")
        return value

    @field_validator('maximum_amount')
    def maximum_amount_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\-?[0-9]+(\.[0-9]+)?$", value):
            raise ValueError(r"must validate the regular expression /^\-?[0-9]+(\.[0-9]+)?$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PackagePriceWithCommitments from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PackagePriceWithCommitments from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "amount": obj.get("amount"),
            "quantityPerPackage": obj.get("quantityPerPackage"),
            "minimumAmount": obj.get("minimumAmount"),
            "maximumAmount": obj.get("maximumAmount")
        })
        return _obj


