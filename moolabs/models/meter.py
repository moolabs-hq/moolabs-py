# coding: utf-8

"""
    Moolabs API

    Unified Moolabs SDK covering billing (CLS) and usage (Meter) operations. Both surfaces share one client and one auth flow.

    The version of the OpenAPI document: 0.0.0-rc2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from moolabs.models.meter_aggregation import MeterAggregation
from typing import Optional, Set
from typing_extensions import Self

class Meter(BaseModel):
    """
    A meter is a configuration that defines how to match and aggregate events.
    """ # noqa: E501
    id: Annotated[str, Field(strict=True)] = Field(description="A unique identifier for the resource.")
    description: Optional[Annotated[str, Field(strict=True, max_length=1024)]] = Field(default=None, description="Optional description of the resource. Maximum 1024 characters.")
    metadata: Optional[Dict[str, StrictStr]] = Field(default=None, description="Additional metadata for the resource.")
    created_at: datetime = Field(description="Timestamp of when the resource was created.", alias="createdAt")
    updated_at: datetime = Field(description="Timestamp of when the resource was last updated.", alias="updatedAt")
    deleted_at: Optional[datetime] = Field(default=None, description="Timestamp of when the resource was permanently deleted.", alias="deletedAt")
    name: Optional[Annotated[str, Field(min_length=1, strict=True, max_length=256)]] = Field(default=None, description="Human-readable name for the resource. Between 1 and 256 characters. Defaults to the slug if not specified.")
    projection_status: Optional[StrictStr] = Field(default=None, description="Async ClickHouse parser reconciliation status for this meter.", alias="projectionStatus")
    projection_error: Optional[StrictStr] = Field(default=None, description="Last projection reconciliation error, if projectionStatus is failed.", alias="projectionError")
    data_health: Optional[StrictStr] = Field(default=None, description="Raw-event extraction health for this meter.", alias="dataHealth")
    data_health_reason: Optional[StrictStr] = Field(default=None, description="Last data-health transition reason.", alias="dataHealthReason")
    slug: Annotated[str, Field(min_length=1, strict=True, max_length=64)] = Field(description="A unique, human-readable identifier for the meter. Must consist only alphanumeric and underscore characters.")
    aggregation: MeterAggregation = Field(description="The aggregation type to use for the meter.")
    event_type: Annotated[str, Field(min_length=1, strict=True)] = Field(description="The event type to aggregate.", alias="eventType")
    event_from: Optional[datetime] = Field(default=None, description="The date since the meter should include events. Useful to skip old events. If not specified, all historical events are included.", alias="eventFrom")
    value_property: Optional[Annotated[str, Field(min_length=1, strict=True)]] = Field(default=None, description="JSONPath expression to extract the value from the ingested event's data property.  The ingested value for SUM, AVG, MIN, and MAX aggregations is a number or a string that can be parsed to a number.  For UNIQUE_COUNT aggregation, the ingested value must be a string. For COUNT aggregation the valueProperty is ignored.", alias="valueProperty")
    group_by: Optional[Dict[str, StrictStr]] = Field(default=None, description="Named JSONPath expressions to extract the group by values from the event data.  Keys must be unique and consist only alphanumeric and underscore characters.", alias="groupBy")
    annotations: Optional[Dict[str, Any]] = Field(default=None, description="Set of key-value pairs managed by the system. Cannot be modified by user.")
    __properties: ClassVar[List[str]] = ["id", "description", "metadata", "createdAt", "updatedAt", "deletedAt", "name", "projectionStatus", "projectionError", "dataHealth", "dataHealthReason", "slug", "aggregation", "eventType", "eventFrom", "valueProperty", "groupBy", "annotations"]

    @field_validator('id')
    def id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[0-7][0-9A-HJKMNP-TV-Za-hjkmnp-tv-z]{25}$", value):
            raise ValueError(r"must validate the regular expression /^[0-7][0-9A-HJKMNP-TV-Za-hjkmnp-tv-z]{25}$/")
        return value

    @field_validator('projection_status')
    def projection_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending', 'ready', 'failed']):
            raise ValueError("must be one of enum values ('pending', 'ready', 'failed')")
        return value

    @field_validator('data_health')
    def data_health_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['unknown', 'healthy', 'degraded']):
            raise ValueError("must be one of enum values ('unknown', 'healthy', 'degraded')")
        return value

    @field_validator('slug')
    def slug_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^[a-z0-9]+(?:_[a-z0-9]+)*$", value):
            raise ValueError(r"must validate the regular expression /^[a-z0-9]+(?:_[a-z0-9]+)*$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Meter from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "id",
            "created_at",
            "updated_at",
            "deleted_at",
            "projection_status",
            "projection_error",
            "data_health",
            "data_health_reason",
            "annotations",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Meter from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "description": obj.get("description"),
            "metadata": obj.get("metadata"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt"),
            "deletedAt": obj.get("deletedAt"),
            "name": obj.get("name"),
            "projectionStatus": obj.get("projectionStatus"),
            "projectionError": obj.get("projectionError"),
            "dataHealth": obj.get("dataHealth"),
            "dataHealthReason": obj.get("dataHealthReason"),
            "slug": obj.get("slug"),
            "aggregation": obj.get("aggregation"),
            "eventType": obj.get("eventType"),
            "eventFrom": obj.get("eventFrom"),
            "valueProperty": obj.get("valueProperty"),
            "groupBy": obj.get("groupBy"),
            "annotations": obj.get("annotations")
        })
        return _obj


